# 부족한 점 분석 (페르소나 기반)

세 페르소나 관점으로 게임을 점검한다.
A = 6년차 트위터 고인물(재미·고증), B = 10년차 개발자(견고함), C = 7년차 디자이너(비주얼).
우선순위: P0=치명 / P1=중요 / P2=개선.

---

## 🔎 데이터 검증 로그 — 웹서치 3라운드 (성격 다른 페르소나)

정답 데이터(`js/data.js`)를 **서치 → 검수 → 반영 → 수정** 절차로 3회 교차검증했다.
각 라운드에 성격이 다른 검수 페르소나를 배정해 서로 다른 유형의 오류를 잡게 했다.

| R | 검수 페르소나 (성격) | 잡은 문제 | 조치 |
|---|---|---|---|
| **R1** | **V1 팩트 아키비스트** — 냉정·1차출처 원리주의 | ① 미니 3집 **Going Seventeen(2016, 붐붐/BOOMBOOM)** 누락 ② **SPILL THE FEELS**를 정규5집/MAESTRO로 오기재 ③ 정규5집 **HAPPY BURSTDAY(2025, Thunder)** 누락 | Going Seventeen 추가 / SPILL THE FEELS → **미니 12집, "LOVE, MONEY, FAME"** 정정 / 17 IS RIGHT HERE(베스트, MAESTRO)·HAPPY BURSTDAY 추가 |
| **R2** | **V2 의심 많은 트위터 고인물** — 리패키지·관용표기 트집 | ④ 내가 임의로 붙인 **"Thunder (썸딩)"** 한글명 오류 ⑤ Pretty U / BOOMBOOM / **Very Nice**(리패키지) 혼동 소지 ⑥ FML **더블 타이틀** 미표기 | "Thunder"로 정정 / 원곡·리패키지 타이틀 확정(예쁘다=정규1집, 아주NICE=리패키지) / FML 더블타이틀 주석 |
| **R3** | **V3 데이터 정합성 엔지니어** — 스키마·포맷·경계값 | ⑦ `anode` 타이틀곡만 **"독: Fear"**로 "국문 (영문)" 패턴 이탈 ⑧ 나머지 8개 초기앨범 타이틀곡 미검증 | **"독 (Fear)"**로 포맷 정규화 / 8개 전부 웹 출처로 확인, 중복 타이틀 0 |

**검증 결과:** 앨범 17 → **20개**(누락 3 보강), 타이틀곡 오류 2건·포맷 1건 정정.
출처: 영문 Wikipedia 각 앨범/곡 문서, Kpop Fandom·carat.fandom Wiki, Apple Music.

> ⚠️ 잔여 리스크: `titleTrack`은 자동 검증 불가한 주관 데이터라, 대규모 확장 시
> 라운드 검증을 반복해야 한다. HAPPY BURSTDAY "Thunder"의 공식 한글 병기는 없음(영문 단독).

---

## 📌 v2 전환 요약 — 포토카드 → 앨범 자켓

초기 버전은 "포토카드로 멤버 맞히기"였으나, **실제 이미지 확보**가 최대 난관이었다
(정품 포카 스캔은 저작권상 리포에 담기 불가). 이를 해결하려고 **공개 앨범 자켓 기반**으로
전환했다.

- 정답 단위: 멤버 카드 → **앨범**
- 문제: 멤버/앨범/연도 → **앨범 / 발매연도 / 타이틀곡** (자켓엔 멤버가 안 나오므로 교체)
- 이미지: 파일 미커밋 → **iTunes CDN URL을 `js/albumArt.js`에 베이크**해 런타임 `<img>` 로드

### 이 전환으로 해결된 이전 지적
| 이전 항목 | 상태 |
|---|---|
| P0 실제로 볼 이미지 부재 | ✅ 해결 경로 확보(`fetch-art.mjs`로 자켓 URL 주입, 파일 재배포 없음) |
| B2 데이터 무결성 검증 없음 | ✅ `data.js` 로드시 검증 + `tests/`에 무결성 테스트 |
| B3 모듈화·전역오염 | ✅ 순수 로직 `js/logic.js` 분리(브라우저/Node 공용), 상태를 DOM에서 분리 |
| B4 자동화 테스트 0 | ✅ `node --test` 단위테스트 9종 도입 |
| B5 상태를 DOM에 저장 | ◑ 완화(도메인 채점을 `state`로 이동, dataset은 UI 표시용만) |
| A2 오답이 너무 티남 | ◑ 부분 개선(중복 제거·연도 근접성은 남음) — 아래 A2 참조 |

---

## 🚨 지금 남은 최상위 (P0/P1)

- **P0-a. `js/albumArt.js` 생성 전에는 여전히 플레이스홀더.** 배포 전 `node scripts/fetch-art.mjs`를
  반드시 1회 실행해야 "자켓 보고 맞히기"가 성립한다. (개발 환경 네트워크 정책상 이 저장소엔
  아직 미생성 상태로 커밋됨 — 사용자 로컬에서 실행 필요)
- **P1-b. 타이틀곡 고증 미검증.** `data.js`의 `titleTrack`은 자동 검증이 불가(주관 데이터)해
  팬덤 확인이 필요하다. 틀리면 페르소나 A가 가장 먼저 잡는다.

---

## 🐦 페르소나 A — 트위터 고인물 / "재미없으면 안 함"

| # | 지적 | 우선순위 |
|---|------|----------|
| A1 | 자켓 퀴즈는 멤버 최애 소구가 약함. 고인물은 "내 최애" 훅이 있어야 붙음 → 향후 멤버 티저 하이브리드 고려 | P2 |
| A2 | 오답 보기가 여전히 무작위. **같은 시기 앨범/비슷한 곡명**으로 근접 오답을 만들어야 난이도가 살아남 (예: 연도 보기를 정답±1~2년으로) | P1 |
| A3 | **결과 공유 카드(트위터용 이미지)가 없음.** 덕질 퀴즈의 핵심은 인증샷. 최우선 재미 요소 | P1 |
| A4 | 자켓만 보면 리패키지/스페셜(예: SECTOR 17 vs Face the Sun) 구분이 매니악해 짜릿함. 이 "함정" 난이도를 의도적으로 설계하면 고인물이 좋아함 | P2 |
| A5 | 8장 고정. 데일리 챌린지·오답 복습·"내 최애 앨범만" 필터 등 리텐션 장치 필요 | P2 |

**A 한 줄:** "자켓 퀴즈 방향은 인정. 근데 오답이 너무 쉽고, 자랑할 결과 이미지 없으면 한 판 하고 끝."

---

## 🖥️ 페르소나 B — 10년차 개발자

| # | 지적 | 우선순위 |
|---|------|----------|
| B1 | `fetch-art.mjs`의 앨범 매칭이 휴리스틱(문자열 유사도+연도). 동명/리패키지에서 오매칭 가능 → 결과 검수 로그와 수동 오버라이드 맵 필요 | P1 |
| B2 | 외부 CDN 의존. iTunes URL이 바뀌면 이미지가 깨짐. `<img> onerror` 폴백은 넣었으나 **주기적 링크 점검/캐시 전략** 부재 | P2 |
| B3 | 여전히 `<script>` 나열식 로드(전역 `window.SVTData` 등). 규모 커지면 ESM+번들러로 이행 권장 | P2 |
| B4 | 접근성 일부만. 정답/오답 `aria-label`은 넣었으나 **키보드 포커스 순서·라이브영역 공지**는 미흡 | P2 |
| B5 | 테스트가 순수 로직/데이터 위주. **DOM 상호작용(채점 마감, 폴백 렌더)**은 Playwright 스모크만 → 브라우저 테스트를 리포에 정식 포함 권장 | P2 |
| B6 | `CONFIG`(라운드·점수)만 코드 상수. 문구는 아직 하드코딩 → i18n/설정 외부화 여지 | P2 |

**B 한 줄:** "로직 분리·테스트·검증은 들어왔다. 이제 외부 이미지 의존의 신뢰성(매칭 오류·링크 깨짐)과 DOM 테스트가 남았다."

---

## 🎨 페르소나 C — 7년차 디자이너

| # | 지적 | 우선순위 |
|---|------|----------|
| C1 | 자켓이 들어오면 주인공 문제는 해결. 단 **플레이스홀더("NO IMAGE")가 밋밋** → 로드 전/실패 시에도 앨범 톤을 암시하는 스켈레톤 필요 | P2 |
| C2 | 웹폰트 미로드(시스템 폴백). `Pretendard` 등 웹폰트 적용으로 인상 통일 | P1 |
| C3 | 정답/오답 **모션 부재**(색 변화만). 정답 팝·오답 셰이크·점수 카운트업으로 쾌감↑ | P1 |
| C4 | 결과 화면 허전. 등급 뱃지+**공유용 비주얼 카드**(A3와 연동) 필요 | P1 |
| C5 | 자켓은 정사각형이라 데스크톱에서 좌측 여백이 큼. **자켓을 더 키우고** 문제를 우측에 밀도 있게 배치하면 몰입↑ | P2 |
| C6 | 다크 전용. 라이트 모드/멤버-앨범별 악센트 컬러 미반영 | P2 |
| C7 | 로딩 상태 UX 없음. 자켓 이미지 로드 중 빈 박스 → 스켈레톤/블러업 처리 필요 | P2 |

**C 한 줄:** "이미지가 들어오면 비주얼의 8할은 자켓이 해준다. 남은 건 폰트·모션·공유카드 — 여기서 스크롤이 멈춘다."

---

## 🎯 다음 스프린트 우선순위 (합의)

**즉시 (배포 성립)**
1. `node scripts/fetch-art.mjs` 실행 → 자켓 URL 베이크 (P0-a)
2. 타이틀곡 팬덤 고증 검수 (P1-b)

**P1 (재미 + 안정)**
3. 결과 공유 카드(이미지/트위터) — A3·C4
4. 근접 오답 로직(연도 ±, 동시기 앨범) — A2
5. `fetch-art.mjs` 오매칭 검수 + 수동 오버라이드 맵 — B1
6. 웹폰트 + 정답/오답 모션 — C2·C3

**P2 (깊이 + 확장)**
7. 데일리 챌린지·오답 복습·최애 필터 — A5
8. DOM 테스트 정식화, ESM 이행, i18n — B3·B5·B6
9. 라이트 모드·스켈레톤·자켓 확대 레이아웃 — C1·C5·C6·C7

---

### 요약 한 줄
> 자켓 전환으로 **P0(볼 이미지)·B(견고함) 대부분이 해소**됐다. 이제 게임의 승부처는
> **재미(근접 오답·공유 카드)와 비주얼(폰트·모션)**, 그리고 **외부 이미지 의존의 신뢰성**이다.
