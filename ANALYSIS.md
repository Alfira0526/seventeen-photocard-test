# 부족한 점 분석 (페르소나 기반)

현재 데모(`index.html` + `js/*` + `css/style.css`)를 세 명의 관점으로 뜯어봤다.
각 항목은 **실제 코드에 근거**하며, 우선순위(P0=치명 / P1=중요 / P2=개선)를 붙였다.

---

## 🚨 공통 최상위 문제 (P0)

**포토카드 이미지가 없어서, "포토카드를 보고 맞히는 게임"이 성립하지 않는다.**
`js/data.js`의 모든 `CARDS[].image`가 `null`이라 `game.js`는 멤버 시그니처 컬러
그라데이션 SVG만 그린다. 즉 지금은 사진을 보고 추론하는 게 아니라 **4지선다 찍기**다.
게임의 존재 이유 자체가 미완성 상태. 세 페르소나 전원이 여기서 걸린다.

> 저작권 때문에 정품 스캔을 리포지토리에 못 넣는 건 맞다. 그렇다면
> ① 팬 제작/직접 촬영 이미지, ② 앨범 콘셉트를 암시하는 2차 일러스트,
> ③ 외부 이미지 URL 주입 방식 중 하나로 **"실제로 볼 것"을 반드시 확보**해야 한다.

---

## 🐦 페르소나 A — 6년차 트위터 고인물 / 다수 팬덤 경력 / "재미없으면 안 함"

핵심 관심: **덕후 고증, 재미, 자랑거리(공유), 반복 플레이 이유.**

| # | 지적 | 근거 | 우선순위 |
|---|------|------|----------|
| A1 | 사진이 없으니 "포카 보고 맞히기"의 손맛이 0. 진성 캐럿이 볼 이유가 없음 | `CARDS[].image=null` | **P0** |
| A2 | 난이도가 너무 쉽고 균일함. 오답 보기가 랜덤이라 티나게 쉬움 (예: 연도 4개가 2015/2022/2018/2023처럼 확 벌어짐) | `buildChoices()`가 전체 풀에서 무작위 추출 | **P1** |
| A3 | 자랑할 결과가 없음. 점수 화면은 있는데 **트위터 공유 카드/이미지가 없음.** 덕질 게임의 생명은 인증샷인데 그게 없음 | 결과 화면에 공유 기능 미구현 | **P1** |
| A4 | "고증" 요소가 얕음. 포카는 보통 **버전(앨범별 A/B/C 커버, 특전, 팬사인회, 럭드)** 개념이 핵심인데 그 축이 아예 없음 | 데이터 모델에 version/특전 없음 | **P1** |
| A5 | 유닛(힙합/보컬/퍼포먼스) 데이터는 넣어놓고 문제로 안 씀. 고인물이 좋아할 "유닛 맞히기" 같은 심화 모드 부재 | `data.js`엔 `unit` 있으나 미사용 | **P2** |
| A6 | 8장 고정이라 금방 질림. 데일리 챌린지/무한 모드/오답 복습 같은 리텐션 장치 없음 | `CONFIG.rounds=8` 고정 | **P2** |
| A7 | 멤버명이 한글 단독이라 뉴비엔 어렵고 고인물엔 심심. 영문/애칭 병기나 최애 필터가 있으면 몰입↑ | `MEMBERS.name`만 사용 | **P2** |

**A의 한 줄 평:** "사진부터 넣어. 그리고 이거 내 타임라인에 자랑할 결과 이미지 없으면 두 번은 안 함."

---

## 🖥️ 페르소나 B — 10년차 시스템 개발자

핵심 관심: **정확성, 데이터 무결성, 확장성, 테스트, 유지보수.**

| # | 지적 | 근거 | 우선순위 |
|---|------|------|----------|
| B1 | 오답 보기 후보가 정답 자신을 포함할 수 있는 엣지 케이스는 막았으나, **보기 개수가 풀보다 크면 조용히 줄어듦.** 방어 로직/검증 없음 | `buildChoices()` slice(n-1)만 | **P1** |
| B2 | **데이터 무결성 검증 부재.** `CARDS[].member`/`album`이 오타로 잘못된 id여도 런타임에서 `memberById[undefined]`로 터짐. 로드시 유효성 체크 없음 | `data.js`에 검증 코드 없음 | **P1** |
| B3 | 전역 오염: `window.SVTData`, IIFE 내부 상태 등. 모듈 시스템(ESM) 미사용이라 번들링/트리셰이킹/재사용 불리 | `data.js` 전역 노출 | **P1** |
| B4 | **자동화 테스트가 저장소에 없음.** 채점 로직(`selectChoice`, `maybeFinishRound`)은 회귀 위험이 큰데 단위 테스트 0. (개발 중 Playwright 스모크만 임시 수행) | 테스트 파일 부재 | **P1** |
| B5 | 채점 상태를 **DOM dataset에 저장**(`dataset.result`)해서 UI와 도메인 로직이 강결합. DOM을 지우면 상태 소실, 테스트도 DOM 필요 | `game.js` selectChoice | **P2** |
| B6 | 접근성/키보드: 보기 버튼이 클릭 전용, 포커스 이동/ARIA 상태 갱신 없음. 스크린리더로 정답/오답 결과 전달 안 됨 | `renderQuestion` | **P2** |
| B7 | i18n·설정 하드코딩. 문구·라운드 수·점수가 코드에 흩어져 있어 A/B 테스트나 다국어 확장 시 손댈 곳 많음 | 문자열 리터럴 산재 | **P2** |
| B8 | 데이터 규모 확장성. 멤버13×앨범17 전량을 카드로 만들면 수백 장인데 지금은 24장 수기 입력. 생성 규칙/시드 데이터 파이프라인 없음 | `CARDS` 수기 배열 | **P2** |

**B의 한 줄 평:** "동작은 하는데 검증·테스트·모듈화가 비어 있다. 데이터가 커지는 순간 손으로 못 버틴다. 스키마 검증과 단위 테스트부터."

---

## 🎨 페르소나 C — 7년차 그래픽 디자이너

핵심 관심: **시각 위계, 타이포, 반응형, 마이크로 인터랙션, 카드 그 자체의 매력.**

| # | 지적 | 근거 | 우선순위 |
|---|------|------|----------|
| C1 | 카드 플레이스홀더가 밋밋한 원+알약 실루엣. **주인공(포토카드)이 가장 매력 없어 보이는 아이러니.** 홀로그램/각도/광택 등 포카 특유의 질감 0 | `placeholderArt()` | **P0~P1** |
| C2 | 폰트가 시스템 폴백. `Pretendard` 지정만 하고 **웹폰트 로드가 없어** 환경마다 인상이 널뜀. 로고 타이포도 기본 산세리프 | CSS `font-family`, `@font-face` 없음 | **P1** |
| C3 | 정답/오답 피드백이 색상 배경 변화뿐. **모션(정답 팝, 오답 셰이크, 점수 카운트업) 부재**로 쾌감이 약함 | CSS transition만 존재 | **P1** |
| C4 | 시각 위계 약함. 세 질문 카드가 동일 스타일로 나열돼 **어디를 먼저 볼지** 안내가 없음. 진행 게이지(막대)도 텍스트뿐 | `.question` 균일, progress=텍스트 | **P1** |
| C5 | 결과 화면이 허전함. 등급 뱃지가 이모지+텍스트뿐, **공유용 비주얼(카드형 결과 이미지)** 없음 (A3와 맞물림) | `showResult()` | **P1** |
| C6 | 모바일 카드가 240px로 축소되며 **주인공이 더 작아짐.** 모바일에선 오히려 카드를 크게 보여줘야 함 | `@media` `.card-stage max-width:240px` | **P2** |
| C7 | 컬러 시스템이 보라-핑크 그라데이션 단일 톤. 멤버별 시그니처 컬러(데이터엔 있음)를 UI 악센트로 못 살림. 다크 전용이라 라이트 대응도 없음 | `:root` 고정 팔레트 | **P2** |
| C8 | 버튼/보기 hit area는 무난하나, 4지선다 2열 그리드가 **긴 앨범명(예: "SEVENTEENTH HEAVEN")에서 줄바꿈/정렬 깨짐** 우려 | `.choices` 1fr 1fr | **P2** |

**C의 한 줄 평:** "포토카드 게임인데 정작 카드가 안 예쁘다. 주인공을 살리는 질감·모션·공유 비주얼이 없으면 스크롤에서 안 멈춘다."

---

## 🎯 우선순위 로드맵 (합의안)

**P0 — 지금 당장 (게임 성립 조건)**
1. 실제로 "볼 수 있는" 카드 이미지 확보 (`image` 채우기 or 외부 URL 주입 방식) — A1·C1
2. 이미지 없을 때의 플레이스홀더도 최소한 앨범 콘셉트를 암시하도록 개선 — C1

**P1 — 다음 스프린트 (재미 + 안정성)**
3. 결과 공유 카드(이미지/트위터) — A3·C5
4. 난이도 로직: 근접 오답(같은 시기 앨범/같은 유닛 멤버) 생성 — A2
5. 데이터 스키마 검증 + 채점 로직 단위 테스트 도입 — B2·B4
6. 웹폰트 + 정답/오답 모션 — C2·C3
7. ESM 모듈화, 상태를 DOM에서 분리 — B3·B5

**P2 — 이후 (깊이 + 확장)**
8. 버전/특전 축, 유닛 맞히기·데일리 챌린지 등 모드 확장 — A4·A5·A6
9. 카드 생성 파이프라인(멤버×앨범 전량) — B8
10. 멤버 시그니처 컬러 UI 반영, 라이트 모드, 긴 텍스트 대응 — C7·C8

---

### 요약 한 줄
> **"돌아는 가는 4지선다 스켈레톤"**은 완성됐다. 하지만 A(재미)·B(견고함)·C(비주얼)
> 모두가 지목하는 **P0 = 진짜 포토카드를 보여주는 것**이 빠져 있어서, 지금은
> "포토카드 게임"이라기보다 "세븐틴 상식 4지선다"에 가깝다. 여기부터 메꿔야 한다.
