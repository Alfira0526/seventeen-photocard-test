# 변경 이력 (CHANGELOG)

이 프로젝트의 주요 변화를 버전별로 정리합니다. 상세 작업 로그는
[PROGRESS.md](PROGRESS.md), 개선 분석은 [ANALYSIS.md](ANALYSIS.md) 참고.

## v0.9.3 — 공유 해시태그 보강(#CARAT) · 전 경로 일관 적용
- 🏷️ 공유 멘트에 **#CARAT**(팬덤 태그) 추가, 로케일별 태그 세트 정비. 공유 문구·클립보드
  복사·**이미지 각인**까지 동일한 해시태그가 항상 들어가도록 `share.tags` 로 일원화.

## v0.9.2 — 공유 앱별 대응 강화 · 원우 사진 교체 · 명예의전당 즉시표시
- 📤 **공유 개선(앱별 한계 대응)**:
  - 결과 이미지에 **태그·사이트 URL을 새겨** 넣어, 텍스트를 지우는 앱(인스타 스토리·카톡)에서도 보이게 함.
  - **트위터**: 모바일은 네이티브 공유로 이미지+캡션(링크 포함) 첨부, 데스크톱은 인텐트에 `url` 파라미터로 링크 포함.
  - **인스타·카톡**: 공유와 동시에 링크·태그를 **클립보드에 복사**(스토리 텍스트에 붙여넣기 안내 토스트).
- 🖼️ **원우 멤버 사진 교체**(요청 반영): 대표 이미지 대신 페이지 이미지 목록에서 다른 솔로 컷 선택.
  나머지 멤버 사진은 기존 그대로 보존(CI 재수집 시 특정 멤버만 교체). 멤버 사진 출처 표기 정정(© kpop.fandom).
- ⚡ **명예의 전당 즉시 표시**: 원격 조회를 기다리지 않고 로컬 기준으로 바로 노출 → 원격(파이어베이스) 지연 영향 제거.
- ℹ️ 데이터 점검: "247"은 <b>퍼포먼스 유닛 곡</b>(호시·준·디에잇·디노)으로 확인 — 원우가 부르지 않는 게 맞아 문제 정상.

## v0.9.1 — 공유 시 이미지+링크 함께 전송 수정
- 🐞 **SNS 공유에서 텍스트만 가던 문제 수정**: 다수 모바일 브라우저는 Web Share 에서
  `files`(이미지)와 `url`(링크)을 **동시에** 못 보내 `canShare`가 실패 → 이미지가 빠지고
  텍스트만 전송됐음. **링크를 캡션(text)에 넣어 이미지와 함께** 전송하도록 변경(그래서
  이미지+링크가 같이 감). 파일 미지원 기기는 텍스트+링크로 폴백.
- ✅ E2E에 공유 페이로드 검증(이미지 포함·링크 포함·url필드 미충돌) 추가(E2E 12).

## v0.9 — 명예의 전당(롤링) · 토스톤 카피 · 처음으로 버튼 · 랭킹 계층 분리
- 🏆 **명예의 전당(시작화면 롤링)**: 모드별 기록이 **10건 이상** 모이면 시작화면에
  상위 순위가 세로로 부드럽게 롤링(마퀴, 상·하단 페이드). 여러 모드가 조건을 채우면
  8초마다 모드가 순환.
- ✍️ **메인 카피를 토스 기획 톤 + 후킹**으로 리라이트(5개 로케일 전부). 예)
  "자켓만 보고 앨범·연도·타이틀곡까지 척척? 진짜 캐럿인지 지금 가려봐요."
- 🔁 **버튼 정리**: ‘한 판 더!’ = 방금 한 모드로 즉시 재시작 / **‘처음으로’ 버튼 추가** = 시작화면 복귀.
- 🗂️ **랭킹 데이터 계층 분리**(`js/ranking.js`): 등록한 **모드의 순위만** 노출.
  로컬 즉시 저장 + **원격 저장소(설정 시) 병합**으로 다른 PC/IP 공유 대비(현재 기본은 로컬,
  `js/ranking-config.js`에 백엔드만 지정하면 전역 공유 활성화).
- ✅ 유닛 24 + E2E 11(명예의 전당 노출/숨김·재시작·처음으로 포함).

## v0.8.1 — 언어 직접 선택 시 영문 병기 해제
- 🔤 언어 선택기에서 **특정 언어를 직접 고르면 영문 병기가 사라지고 단독 표기**로 전환.
  자동 감지(접속 지역/브라우저)로 들어온 방문자는 그대로 병기(지역어 + 영문) 유지.
  직접 고른 언어는 기기에 저장되어 다음 방문에도 단독 표기.

## v0.8 — 유튜브 솔로 MV 문제 + 지역언어/공유 개선
- ▶️ **유튜브 MV 썸네일 문제 추가**: 유튜브 공개 솔로곡(HOSHI-Spider·WOOZI-Ruby·
  VERNON-Black Eye·DINO-Wait)의 공식 MV 썸네일을 보고 곡·발매연도를 맞혀요.
  영상 ID는 HYBE LABELS 공식 채널로 검증(YouTube Data API), 썸네일은
  `img.youtube.com` 핫링크(고화질 실패 시 hq 자동 폴백) + ▶ 배지.
- 🌏 **접속 지역(IP) 언어 우선 적용**: 브라우저 언어보다 IP 지역을 우선하도록 보정
  (엔드포인트 다중화·타임아웃 단축, 플레이 중에도 반영). 직접 고른 언어는 계속 우선.
- 📤 **공유 개선**: Web Share 에 이미지 + 설명 + 링크를 함께 담아, 대상 앱이 지원하는
  범위에서 이미지와 링크가 함께 공유되도록(파일 미지원 시 텍스트+링크로 폴백).
- ✅ 유닛 24(유튜브 문제 무결성 포함) + E2E 7.

## v0.7 — 접속 지역 자동 번역 + 영문 병기(다국어)
- 🌍 **접속 지역(IP) 기반 언어 자동 선택**: `js/geo.js` 가 무료 IP 지역 API(ipapi.co 등)로
  국가를 감지해 해당 지역 언어로 전환. 실패/차단 시 브라우저 언어 → 영어로 조용히 폴백.
  우측 상단 **언어 선택기**로 수동 변경 가능(선택은 기기에 기억).
- 🗣️ **영문 병기**: 지역 언어를 주(主)로, 영어를 보조로 함께 표기. 문제·보기·결과·시작화면·
  공유 문구까지 UI 전반에 적용(영어 로케일은 단일 표기).
- 🈶 **5개 로케일**: 한국어 · English · 日本語 · 简体中文 · Español. `js/i18n.js` 를 병기
  엔진으로 재작성(로케일 원본 + 병기 결합기, `setLocale` in-place 갱신).
- 🧑‍🎤 멤버 이름 로마자(en) 표기 추가 → 영문/기타 로케일에서 자연스러운 병기.
- ✅ 유닛 23 + E2E 7(병기/단일언어 전환 검증 추가). 조사(이/가·와/과)는 한국어에서 유지.

## v0.6 — 모드 개편 · 100점 채점 · 로컬 랭킹 · 조사 자동화
- 🕹️ **모드 3종으로 개편**: 오늘의 챌린지(데일리) 제거, **무한 모드**(틀리면 끝) ·
  **타임어택**(60초 동안 최대한, 봐주는 목숨 3개) 추가. 일반은 랜덤 20문제 유지.
- 💯 **채점을 100점 만점으로 정리**(문제당 5점). 무한/타임어택은 맞힌 개수·연속 기록으로 집계.
- 🏆 **로컬 랭킹**: 이름을 정해 결과를 등록하면 기기(localStorage)에 모드별 순위로 남아요(상위 10).
- ✍️ **조사 자동화**: 멤버 이름이 문제에 나올 때 받침에 맞춰 이/가·와/과를 골라줘요
  (예: "도겸가" → "도겸이"). 한글 받침 계산(`(코드-0xAC00)%28`)으로 처리.
- 🖼️ **자켓 미로드 수정**: 5번째 정규 앨범 *HAPPY BURSTDAY*(2025) 커버를 iTunes
  collectionId(1846801259) OVERRIDE로 고정 → CI 재베이크 시 27/27 확보.

## v0.5.1 — 결과 SNS 공유 구조화
- 📤 **공유하기(Web Share API)**: 모바일 네이티브 공유 시트로 인스타·카톡·트위터 등
  어디든 결과 이미지를 바로 첨부해 공유(핵심 경로). 미지원(데스크톱)은 저장+안내로 폴백.
- 개별 버튼: 🖼️ 이미지 저장 / 🐦 트위터(인텐트+이미지 저장) / 📷 인스타(Web Share·저장 안내) /
  💬 카카오톡(Kakao SDK — JS 키 설정 시 링크 공유, 아니면 Web Share·저장 안내).
- 토스트 안내 + 멤버 사진은 kpop.fandom에서 13/13 수집(핫링크).

## v0.5 — 멤버 사진 라운드 + 문제 유형 대폭 확장
- 🧑‍🎤 **멤버 사진 라운드 추가**(얼굴 맞히기는 제외 — 너무 쉬움):
  - M1 이 멤버가 부른 유닛곡 / M2 부르지 않은 곡 / M4 다른 유닛인 멤버 / M5 작사한 타이틀곡
  - 덱에 앨범 카드 + 멤버 카드를 섞어 출제. 멤버 사진은 Wikimedia Commons(자유 라이선스)
    에서 CI로 수집(`fetch-members.mjs`), 없으면 유닛 컬러 아바타. 문제에 멤버 이름 병기로 오매칭 방어.
- 🎵 **앨범 문제 확장**: A1 유형(미니/정규…) / A3 나중에 나온 앨범 / A4 몇 집
- 🏷️ 문제 유형별 난이도(★~★★★★) 표기. 각 유형 **정답·오답 사실성 검수**(유닛 테스트 23개).
- 🐞 재로딩 버튼이 항상 보이던 CSS 버그 수정(`.btn-reload[hidden]`).

## v0.4.4 — 자켓 로딩 구조 개선(런타임 의존 제거) · **26/27 커버 베이크 완료**
- ⚙️ **GitHub Actions 자동 베이크**(`.github/workflows/fetch-art.yml`): CI가 GitHub 서버에서
  커버 URL을 뽑아 `js/albumArt.js`로 자동 커밋 → 배포 사이트는 런타임 iTunes 호출 없이
  애플 CDN에서 `<img>`로 직접 로드(오매칭·레이트리밋 제거). **결과: 26/27개 커버 확보**.
- 🐞 근본 원인 발견(CI 로그): `country=kr`면 iTunes가 아티스트명을 **한글("세븐틴")**로 반환 →
  영문 "SEVENTEEN" 필터가 전부 탈락(0/27). → **country=us**(영문명) + 세븐틴 별칭 + 확인된
  collectionId OVERRIDE로 해결.
- 🪶 런타임 폴백 경량화: 덱 일괄 프리페치 제거 → **다음 1장만 선로딩**, 재시도 제거(요청 폭주·429/403 완화).
- 세 페르소나(팬/개발자/디자이너) 교차검증 결론 반영: "런타임 실시간 의존 제거".
- 참고: happyburstday(2025) 1개만 런타임 폴백. 멤버 믹스테이프(Spider/Ruby/Black Eye/Wait)는 US 스토어에 있어 정상 베이크됨.

## v0.4.3 — 자켓 오매칭 수정 · 오답 복습 제거
- 🖼️ **자켓 오매칭 버그 수정**: 솔로/유닛 릴리스(제목이 흔함)가 남의 앨범 커버를
  불러오던 문제(예: Vernon→"Nicholas Vernon", Bittersweet→가면라이더 OST). 아티스트
  **정확 매칭**(부분포함 금지) + 제목 강일치(임계 0.6)로 바꿔, 틀린 이미지 대신 플레이스홀더 표시.
  솔로/유닛은 'SEVENTEEN' 문맥을 더한 검색어로 정확 릴리스 노출 확률↑.
- 🗑️ **오답 복습 모드 제거**(사용자 요청).

## v0.4.2 — 릴리스 확장(스페셜/유닛/믹스테이프/싱글) + 버그픽스
- 앨범 20 → **27개** 추가(모두 웹 검증):
  - 스페셜: Director's Cut(2018, 고맙다/THANKS)
  - 유닛: BSS SECOND WIND(2023, 파이팅 해야지)
  - 믹스테이프: Hoshi Spider(2021)·Woozi Ruby(2022)·Vernon Black Eye(2022)·Dino Wait(2023)
  - 디지털 싱글: Wonwoo·Mingyu Bittersweet(2021)
- 솔로/유닛 자켓 로딩 위해 `artist` 필드 지원(artlive·fetch-art 매칭에 반영)
- 🐛 시작화면 "8장" → "10장" 오기 수정 / You Made My Dawn 타이틀곡 `Home` 정정

## v0.4.1 — 공유·이미지 로딩 버그픽스
- 🐦 **트위터 공유**: `window.open`의 features 인자로 작은 팝업 창이 떠 로그인이
  불편하던 문제 → **anchor(target=_blank) 클릭으로 정상 탭** 오픈. 공유 시 결과 이미지도
  자동 저장하고 안내 문구 추가.
- 🖼️ **자켓 로딩 안정화**: 타임아웃 6→8초, **네트워크 오류 재시도**, **대체 검색어**(힌트→제목),
  **성공만 캐시**(실패는 다음에 재시도), 시작 시 **덱 프리페치**, `<img>` 로드 실패 1회 재시도.
- E2E에 트위터 공유(새 탭) 테스트 추가 → 유닛 19 + E2E 4.

## v0.4 — 문제 유형 확장 & 카드당 1문제
- 게임 방식 변경: 카드당 **랜덤 유형 1문제**(기존 3문제 고정) · 10장 · 만점 100점
- 문제 엔진 `js/questions.js` 신설: 유형 레지스트리 + 가용성 게이팅 + 가중 랜덤
- 고난도 유형 3종 추가(데이터 있는 앨범만 출제):
  - `notInAlbum` 수록곡이 아닌 것 / `lyricist` 타이틀곡 작사 멤버 / `unitSong` 유닛곡
- 웹서치 검증 데이터 추가: 수록곡+유닛 태그(5앨범), 타이틀곡 작사 멤버(4앨범)
- 단위 테스트 19 + E2E 3, 문서 대폭 보강(README/ARCHITECTURE/DATA_GUIDE)

## v0.3 — 재미·안정·비주얼
- **3모드**: 일반 / 데일리 챌린지(날짜 시드 결정론 + 스트릭) / 오답 복습
- **결과 공유 카드**: canvas PNG 다운로드 + 트위터 인텐트
- **근접 오답**: 같은 시기·유형 앨범, 가까운 연도 우선
- 라이트/다크 테마 토글, 정답 pop/오답 shake, 점수 카운트업, 이미지 스켈레톤+블러업
- Pretendard 웹폰트(CDN 폴백), 접근성 라벨, `prefers-reduced-motion`
- 자켓 **실시간 로딩**(`artlive.js`, iTunes JSONP) → 무설치로 커버 자동 표시
- 순수 로직 분리(`logic.js`)·i18n-lite(`i18n.js`)·E2E 테스트 리포 편입

## v0.2 — 포토카드 → 앨범 자켓 전환
- 저작권 안전 + 이미지 확보를 위해 **앨범 자켓 기반**으로 전환
- 문제: 멤버 → **앨범/연도/타이틀곡**
- 이미지: 파일 미커밋, **iTunes CDN URL**을 런타임 로드(`fetch-art.mjs`로 베이크 가능)
- 앨범 데이터 **웹서치 3라운드 교차검증**(앨범 20개): 누락 보강·오기재 정정
- 데이터 무결성 검증 + 단위 테스트 도입

## v0.1 — 초기 데모
- 세븐틴 **포토카드 퀴즈**(멤버/앨범/연도 맞히기) 스켈레톤
- 게임 루프·채점·결과·재시작, 다크 UI, SVG 플레이스홀더
- 세 페르소나(팬/개발자/디자이너) 관점 개선 분석([ANALYSIS.md](ANALYSIS.md))
