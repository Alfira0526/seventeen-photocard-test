# 주간 운영 리뷰 — 2026-07-26

## 0) 데이터 가용성 — **데이터 없음**

이번 주는 텔레메트리·제보 스냅샷이 **존재하지 않습니다.**

- `docs/ops/SNAPSHOT.md` — 없음
- `docs/ops/stats-latest.json` — 없음
- `docs/ops/reports-latest.json` — 없음
- `docs/ops/handled.json` — 존재하나 빈 배열(`[]`), 처리 이력 없음

**원인 확인**: GitHub Actions에서 `ops-snapshot.yml` 워크플로 실행 이력을 조회한 결과
**실행 횟수 0회**입니다. 워크플로 자체는 2026-07-26(오늘, 일요일) 07:19 KST에 이 브랜치에
새로 생성·등록되었고, cron 스케줄은 `10 0 * * 1`(매주 월요일 00:10 UTC)이므로
**첫 실행이 아직 도래하지 않았습니다**(다음 실행: 내일 월요일). 즉, 오류나 장애가 아니라
운영 루프가 이번 주에 막 구축되어 첫 수집 주기를 기다리는 정상 상태입니다.

**파이프라인 자체 점검(개발자 관점, 코드 리딩)**: 데이터가 없는 김에 수집 경로 자체의
배선 상태를 확인했습니다.
- `js/analytics.js`, `js/report.js` 가 `index.html`에 정상 포함됨 (173-174행).
- `SVTAnalytics.play/finish/answer` 가 `js/game.js`(142, 356, 458행)에서 실제 게임 진행
  이벤트에 연결되어 있음을 확인.
- `SVTReport.submit` 이 `js/game.js`(644행)에서 오류 제보 UI와 연결되어 있음을 확인.
- Firebase 엔드포인트는 `js/ranking-config.js`에 설정되어 있어(`svt-pctest-default-rtdb`)
  `ops-snapshot.mjs`가 "Firebase 미설정" 사유로 조기 종료될 가능성은 낮음.
- 결론: 배선상 문제는 발견되지 않음. 월요일 첫 스냅샷 실행 결과를 다음 주 리뷰에서 확인 필요.

이에 따라 이번 주는 아래 4)~6)의 **제안**만 남기고, 이용률/정답률 분석·오류 데이터 수정은
스냅샷이 확보되는 다음 주기로 미룹니다(사실 미확인 상태에서 임의 수정은 안전 규칙 위반).

## 1) 이용률·정답률 요약

데이터 없음 — 위 0) 참고. 다음 주(2026-08-02 예정 리뷰)부터 `docs/ops/SNAPSHOT.md` 기반으로
모드별 이용률(plays/games), 유형별 정답률, 이상문제 후보(표본 8+ & 정답률 저조)를 정리합니다.

## 2) 오류 제보 교차검증 및 처리 내역

처리한 오류 없음(제보 데이터 없음). `docs/ops/handled.json` 변경 없음.

> 참고: 과거(이 운영 루프 도입 이전) 수동으로 확증·반영된 사례 — "Face the Sun 타이틀곡
> Darl+ing → HOT" (커밋 `30e0ce7`). 이번 주 자동 루틴이 신규로 확증한 항목은 없습니다.

## 3) 이상문제 후보

데이터 없음(표본 확보 전).

## 4) 5인 페르소나 리뷰

### 팀장(8년차) — 종합
이번 주는 실질 지표가 없는 "구축 직후 대기" 주간입니다. 최우선 과제는 (a) 월요일
`ops-snapshot` 첫 실행이 정상적으로 커밋되는지 확인, (b) 실패 시 원인(Firebase 규칙 미게시
등) 파악입니다. `OPS_ROUTINES.md`의 Firebase 규칙 안내(§ Firebase 보안 규칙)가 실제로
Realtime Database 콘솔에 게시되었는지는 리포지토리 파일만으로는 확인 불가 — **사용자
확인이 필요한 유일한 액션 아이템**으로 남깁니다. 코드 변경은 이번 주 없음(데이터 근거 없이
수정하지 않는다는 안전 규칙 준수).

### QA(4년차) — 이용률/정답률·제보
분석할 표본이 없어 이번 주는 수치 리포트를 생략합니다. 대신 제보 UX 경로(결과화면 제보
링크, 라벨형 버튼)가 최근 커밋(`5ec3f70`)으로 강화된 점을 확인했고, 실제 제보가 들어오는지는
다음 스냅샷에서 확인 예정. **제안**: 스냅샷이 비어 있을 가능성(초기 이용자 수가 적어 표본
부족)에 대비해, 다음 리뷰부터는 "표본 부족"과 "장애로 인한 미수집"을 구분할 수 있게
`SNAPSHOT.md`에 마지막 성공 실행 시각을 명시하면 좋겠습니다(제안, 미반영).

### 개발자(4년차) — 시스템 오류(로딩/정합성/성능)
텔레메트리·제보 배선은 코드 레벨에서 이상 없음(위 0) 참고). `node --test` 기준 37개 테스트
중 24 pass / 13 skip / 0 fail — 스킵은 대부분 `notInAlbum`/`unitSong`/`lyricist` 데이터가
아직 없는 앨범에 대한 조건부 테스트로 추정되며 회귀는 아님. 이번 주 코드 변경 없음.
**제안**: `ops-snapshot.yml`은 `contents: write` 권한으로 커밋을 직접 push하는데, 첫 실행이
실패할 경우(예: 브랜치 보호 규칙) 조용히 실패할 수 있어 Actions 탭에서 1회 수동
`workflow_dispatch`로 사전 검증해보는 것을 권장(제안, 미반영 — 실행은 사용자 판단).

### 기획자(4년차) — 재미 요소·기획의도 유지
현재 게임은 자켓·멤버·MV 퀴즈 + 랭킹이라는 최초 기획의도를 유지하고 있으며 최근 커밋들
(유닛 MV 문제, 언어별 병기, 명예의 전당)로 콘텐츠가 꾸준히 확장 중입니다. `DATA_GUIDE.md`
기준 `notInAlbum`/`unitSong`/`lyricist` 커버리지가 일부 앨범에만 있어 문제 다양성이
앨범별로 편차가 있습니다.
**신규 문제 아이디어(제안, 미반영)**:
- "이 가사, 누가 썼을까" 외에 "이 파트, 누가 불렀을까"(유닛/포지션 매칭) — `unitSong`
  커버리지 확장과 함께 고려.
- 발매 연도 순서 정렬형 문제(타임라인 퀴즈) — 기존 `laterAlbum` 유형과 톤이 비슷해 자연스럽게
  확장 가능.
- 앨범 자켓 부분 크롭(확대) 보고 맞추기 — 난이도 상향 모드로.
이상은 모두 신규 기능 성격이라 이번 주는 반영하지 않고 제안으로만 남깁니다.

### 디자이너(3년차) — UX/사용자 친화성
제보 접점이 최근 라벨형 버튼 + 결과화면 링크로 강화되어(`5ec3f70`) 발견성이 개선된 것으로
보입니다. 실사용 데이터가 없어 추가 UX 이슈는 이번 주 식별 불가.
**제안(미반영)**: 스냅샷이 비었던 첫 주차라, 다음 주 실데이터 확보 후 "정답률이 유독 낮은
문제 유형"이 실제로 UX(문항 표현 모호성) 문제인지 데이터 오류인지 구분해서 보고하면
팀장 판단에 도움이 될 것 같습니다.

## 5) 개선 제안 요약 (사용자 승인 대기 — 이번 주 미반영)

| 구분 | 제안 | 담당 관점 |
|---|---|---|
| 운영 | `ops-snapshot` 1회 수동 실행(workflow_dispatch)으로 파이프라인 사전 검증 | 개발자 |
| 운영 | `SNAPSHOT.md`에 마지막 성공 수집 시각 명시(장애/무데이터 구분) | QA |
| 콘텐츠 | 유닛/포지션 매칭 문제, 발매연도 타임라인 퀴즈, 자켓 크롭 난이도 모드 | 기획자 |
| UX | 실데이터 확보 후 "정답률 저조 = 데이터오류 vs UX모호성" 구분 보고 | 디자이너 |

## 6) 이번 주 변경 사항

없음(코드 수정 없음, `docs/ops/handled.json` 변경 없음). 리포트 파일만 신규 추가.
